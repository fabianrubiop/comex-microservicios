server:
  port: 8084

spring:
  application:
    name: documentos-ms
  security:
    user:
      name: admin
      password: comex123
  datasource:
    url: jdbc:mysql://mysql-comex:3306/documento_db
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
  flyway:
    enabled: true
    repair-at-start: true

eureka:
  client:
    service-url:
      defaultZone: http://admin:comex123@eureka-server:8761/eureka/

springdoc:
  api-docs:
    path: /v3/api-docs