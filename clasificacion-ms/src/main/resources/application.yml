server:
  port: 8082

spring:
  application:
    name: clasificacion-ms
  security:
    user:
      name: admin
      password: comex123
  datasource:
    url: jdbc:mysql://localhost:3306/clasificacion_db
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  flyway:
    enabled: false # Usamos JPA auto-update para simplificar en el examen

eureka:
  client:
    service-url:
      defaultZone: http://admin:comex123@localhost:8761/eureka/